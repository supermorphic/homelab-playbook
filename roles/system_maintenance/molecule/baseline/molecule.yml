---
dependency:
  name: galaxy
  enabled: false

driver:
  name: default
  options:
    managed: true

platforms:
  - name: debian13
    hostname: molecule-debian13
    groups:
      - os_managed
    image: localhost/homelab-playbook-system-maintenance-baseline-debian13:local
    container_name: homelab-playbook-system-maintenance-baseline-debian13
    container_command: /usr/lib/systemd/systemd
    container_privileged: false
    container_systemd: always
    restart_policy: always
    pull: never
    user: ansible
    labels:
      io.supermorphic.homelab-playbook.repository: homelab-playbook
      io.supermorphic.homelab-playbook.scenario: system_maintenance/baseline
      io.supermorphic.homelab-playbook.platform: debian13
provisioner:
  name: ansible
  log: true

verifier:
  name: ansible

scenario:
  test_sequence:
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - destroy
