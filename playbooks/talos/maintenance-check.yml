---
- name: Check one Talos node for maintenance
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  roles:
    - role: talos_lifecycle
      vars:
        talos_lifecycle_action: maintenance-check
